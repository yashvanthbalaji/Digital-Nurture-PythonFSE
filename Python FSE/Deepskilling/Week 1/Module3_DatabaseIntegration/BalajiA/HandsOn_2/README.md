# HANDS-ON 2: Writing SQL Queries — DML, Joins & Aggregations

**Author:** Balaji A

## Topic

DML Operations, SELECT Queries, Joins, and Aggregate Functions in `college_db`.

## Output

> **Screenshots of the following are attached below:**

* Insert, Update, and Delete operations
* Row count verification after each DML step
* Filtered SELECT query results
* INNER JOIN and LEFT JOIN query outputs
* Multi-table JOIN outputs
* Aggregate function results using `COUNT`, `AVG`, `SUM`, and `MAX`
* `GROUP BY` and `HAVING` query results

## Result

* Successfully inserted, updated, and deleted records in the database.
* Retrieved student, course, professor, and enrollment data using filtering and sorting.
* Used JOIN queries to combine data from multiple tables.
* Calculated summary reports using aggregate functions.
* Verified query results using `GROUP BY` and `HAVING`.
* Gained practical experience in SQL DML, joins, and aggregation.
